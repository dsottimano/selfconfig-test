# Dependencies & build output
node_modules/
dist/
.astro/

# Lanza CMS build output — the admin app, regenerated from admin/ by the deploy
# build (`npm run build:admin`). Not committed; regenerated, never committed.
public/admin/

# EmDash runtime (regenerated by `npx emdash dev` — seed re-applies)
data.db
uploads/

# Secrets
.env
.env.*

# OS / editor
.DS_Store
*.log

# Tooling working dir
.superpowers/

# Original (non-Lanza-format) theme source bundles — inputs, not deliverables
themes/laperle-theme.tar.gz

# Broker lives in its own private repo — never commit here
lanza-broker/
