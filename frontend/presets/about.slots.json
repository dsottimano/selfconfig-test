{
  "name": "about",
  "label": "About",
  "description": "A centered story page — hero, narrative, stat row, and a closing call to action.",
  "slots": [
    { "name": "eyebrow", "label": "Eyebrow", "widget": "string", "required": false },
    { "name": "headline", "label": "Headline", "widget": "string" },
    { "name": "subheading", "label": "Subheading", "widget": "text", "required": false },
    { "name": "storyHeading", "label": "Story heading", "widget": "string", "required": false },
    { "name": "storyBody", "label": "Story", "widget": "text", "required": false },
    {
      "name": "stats",
      "label": "Stats",
      "widget": "list",
      "required": false,
      "fields": [
        { "name": "value", "label": "Value", "widget": "string" },
        { "name": "label", "label": "Label", "widget": "string" }
      ]
    },
    { "name": "ctaHeading", "label": "CTA heading", "widget": "string", "required": false },
    { "name": "ctaText", "label": "CTA text", "widget": "text", "required": false },
    { "name": "ctaButtonText", "label": "CTA button text", "widget": "string", "required": false },
    { "name": "ctaButtonUrl", "label": "CTA button URL", "widget": "string", "required": false }
  ]
}
